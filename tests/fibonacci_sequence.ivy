function fib_sequence(n) {
  function fibonacci(n) {
      if(n <= 1) return 1;
      return fibonacci(n-1) + fibonacci(n-2);
  }
  a = 0;
  while (a <= n) {
    fibonacci(a);
    a += 1;
  }
  return true;
}

fib_sequence(20);
